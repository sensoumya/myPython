import random


def sh_sort(arr):
    n = len(arr)
    gap = n // 2
    while gap > 0:
        for start in range(gap):
            for i in range()
        gap = gap // 2


arr = random.sample(range(1, 100), 20)
print(arr)
print(sh_sort(arr))
